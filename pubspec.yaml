name: flutter_logger
description: "custom logger for flutter projects"
version: 0.0.3
homepage: https://github.com/Anfet/flutter_logger.git

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter


  logger: ^2.2.0
  sprintf: ^7.0.0
#  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
