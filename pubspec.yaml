name: flutter_logger
description: "custom talker config"
version: 0.0.6
homepage: https://github.com/Anfet/flutter_logger.git

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  talker: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
